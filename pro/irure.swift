/// Creates a button with an action and a label.
Button(action: {
    // Action to perform when the button is tapped
    print("Button was tapped")
}) {
    // Button label
    Text("Tap me")
}
